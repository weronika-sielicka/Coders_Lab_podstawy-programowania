/*
function multiplyByTen(number) {
  console.log(number * 10);
}

multiplyByTen(456);
*/
/*
var text = "";

function FooBar(number) {
  var text = "";

for(var i = 1; i <= number; i++){
  if (number%15 === 0){
    text += "FooBar";
  } else if (number%5 === 0) {
    text += "Bar"
  } else if (number%3 === 0) {
    text += "Foo";
  } else {
    text += i;
  }
  }
}
console.log(text);
}

console.log(FooBar(25));


///funkcje ROZWIAZANE
function FooBar(number) {
  var text = "";
  for(var i = 1; i <= number; i++) {
        if(i % 15 == 0) {
          text += "FOOBAR";
        } else if (i % 3 == 0) {
          text += "Foo";
        } else if (i % 5 == 0) {
          text += "Bar";
        } else {
          text += i;
        }
  }
  console.log(text);
}

FooBar(15);
*/

//Obliczanie potęgi
/*
function power(a, n){
  var pwr = 1;
  for(var i = 1; i<=n; i++){
      pwr *= a;
    }
  console.log(pwr);
}

power(4, 4);
*/

function miksowanie(string){
console.log("Zmiksowana " + string);
  }

  mikskowanie("marchewka");


  
